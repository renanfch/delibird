server:
  port: 8080

delibird:
  database:
    url: jdbc:h2:nio:./db/delibird_db;MODE=MySQL
    user: root
    password: 123456
    driver: org.h2.Driver

spring:
  h2:
    console:
      enabled: false
  profiles:
    active: default
  flyway:
    url: ${delibird.database.url}
    user: ${delibird.database.user}
    password: ${delibird.database.password}
    baseline-on-migrate: true
    validate-on-migrate: false
    enabled: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    username: ${RABBITMQ_USER:rabbitmqUser}
    password: ${RABBITMQ_PASSWORD:rabbitmqPass}
