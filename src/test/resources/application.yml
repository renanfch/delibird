server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:delibird_test_db;MODE=MySQL
    username: root
    password: 123456
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: false
  profiles:
    active: default
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    virtual-host: "delibird"
